# Changelog

## 0.13.0 — 2026-09-23

- 每阶段独立 review/gate 通过、记录同步后默认自动本地提交，保护原 staged/同文件他人改动，提交失败明确受阻；用户禁止提交优先，push 单独遵守授权。
- 持久任务/产品记录改为 `docs/longdev/`、`docs/autopilot/`，随阶段提交；原始日志、图片与基线副本放 `.work/`，持久记录包含可跨机器复核的摘要/manifest。
- 新版技能启动当前工作区时运行实际迁移脚本，保留旧目录保护副本；支持冲突预检、忽略检查、独占锁、原子写入、中断恢复与持久完成标记。`status --check` 只读，更新插件不扫描其它工作区，不自动接手旧任务。
- 新增迁移正常/失败/重复/中断/路径/跨机器/并发测试，三端安装包验证包含脚本；Windows 锁实现与 reparse 防护已提供，真实 Windows 环境仍须实测。

## 0.12.0 — 2026-09-22

- 增加 Codex 插件清单，复用 `skills/` 作为跨客户端共享底层。
- 增加三端共享安装器：保存完整 skills/agents/版本 bundle，生成项目薄入口，校验内容指纹并保护既有文件；dsh PowerShell 只包装同一实现。
- 保持 Claude、Codex、dsh 的运行时入口独立，任务状态和 longdev 协议只有一份。
- 角色调度使用可用客户端工具；缺独立审查能力保持待检查，不以同会话角色切换降级。
- 新增 URL 安装代理指南、安装/升级/冲突行为测试；模型继承 default/强档，私有化视觉每张图片独立会话。

## 0.11.0 — 2026-09-22

- 增加跨会话运行时适配，支持 Claude Code CLI、Codex CLI 和 dsh 的新会话/恢复能力边界。
- 增加 `context/current.md` 交接契约、会话切换状态机和运行器职责边界。
- 明确客户端 resume、fork、退出码不能替代 longdev 的实际产物核对、review、gate 和验收。

发版流程：改代码 → 更新本文件 → 同步 bump `.claude-plugin/plugin.json` 与 `.codex-plugin/plugin.json` 的 `version`（SKILL.md 开工从前者读取）→ 按授权 commit/tag/push；Codex 本地插件更新按当前安装流程使用版本 cachebuster 并保持两份清单一致。
其他设备只有在 `version` 字符串变化后才会收到更新（`/plugin update longdev@zzm-plugins` 或自动更新）。

## 0.10.1 — 2026-09-21

- 针对用户反馈的 DeepSeek v4 私有化图片输入限制，主会话保持纯文字，图片通过独立视觉服务或隔离临时会话处理；限制随模型配置生效，不限客户端。
- 明确历史图片累计计数、目标多模态能力检查及含图历史恢复规则，不向不支持的目标发图，不以子 agent 默认具备隔离能力。
- longdev / autopilot 共享视觉证据和纯文字交接规则；独立审查、阶段及最终 gate 保留视觉验收要求，不以 OCR 替代版式检查。
- 文档、技能格式和静态场景检查通过；尚未在该私有化部署实测规则效果。

## 0.10.0 — 2026-09-21

- longdev 与 autopilot 共用 execution-protocol：稳定需求/验收 ID、状态责任、包含未跟踪文件的基线、对应产物状态的证据及原始日志。
- 实现只进入待检查；独立 review 与主会话 gate 通过后写检查通过，整体交付保持待验收，按有效授权记录用户或代理验收。中断恢复核对实际产物和证据，不依赖旧完成标签。
- 验收逐条核对预期 ID 全集；漏项、重复、未知项和过期证据不能因零失败计数被放行。决策引起变更后重新检查受影响部分。
- 按风险、耦合与可验证性拆阶段；移除固定尺寸停机、重复读取禁令、吞 stderr、模型自动降级和统计差异直接指控造假的规则。
- 沿用已有授权，范围外观察不自动成为任务；失败隔离传播依赖，撤回须授权且保护既有改动。区分目标达成、预算停止、候选耗尽和受阻。
- 本地文档一致性与场景推演检查见 harness 的任务记录；未将其表述为 Claude 多 agent 端到端实跑或长期收益验证。

## 0.9.0 — 2026-09-10

**动机**：longdev 是收敛的执行机——一个任务、有限阶段、单点 HIL。这一版在它上面加一层发散-收敛循环：给一个**产品目标**，确认一次宪章，之后自主迭代到收官，全程无人值守。新 skill 叫 `autopilot`（longdev 的自动挡），执行层每轮迭代就是一个标准 longdev 任务，gate 纪律一条不松。

- **新 skill `skills/autopilot/`**：宪章 → backlog → 迭代（= longdev 任务）→ 验收 → 停机判定的循环。产品层落盘在 `.claude/autopilot/<产品名>/`（CHARTER.md / BACKLOG.md / iterations/ / acceptance/），迭代任务照旧在 `.claude/longdev/<产品名>-i<N>/`。
- **原则一句话：宪章是唯一的人。** 立宪确认（唯一必经 HIL）后，longdev 所有「问用户」的分支按替换表改道：计划确认由 acceptance 按宪章代核；decider 的裁决依据扩成「宪章+PLAN+代码事实」，仍判不了的 B 类选可逆且范围最小的选项备案、C 类（不可逆）没有宪章授权就隔离，**永不猜**。
- **立宪是问卷不是作文**：product-owner 起草宪章后出选择题（固定六维：目标用户/形态/功能范围/质量档位/预算停机/授权边界 + 调研自出题），主会话转 AskUserQuestion 分批问——推荐项排第一、范围类多选、没勾的自动进「非目标」、全选推荐也能开工。用户的输入成本从「写需求文档」降到「点选择题」，但最终确认草稿全文这一步不省。
- **防功能爆炸靠结构不靠措辞**：backlog 每条必须引用宪章条款编号，引不出的不入表；每轮功能数 ≤ 宪章上限；减法项与加法同权；acceptance 只对宪章负责、引不到条款的想法进「观察」永不自动转功能；「非目标」显式成文（范围题没勾的全落这里）。
- **自愈 = 有界失败 + 回滚 + 隔离**：修复两轮仍阻塞 → implementer 按改动清单回滚到阶段快照（`git diff --stat <SNAP>` 为空为准）、该功能 🚫 隔离、其余继续；同一功能永不静默重试，解禁唯一途径是修宪。**熔断**（同一功能二次隔离 / 单轮隔离过半）→ 停机等人，不硬跑。
- **停机条件显式**：宪章验收清单全绿 / 迭代上限 / 全部候选低于阈值 → 收官；收官报告让用户十分钟复核完全部代拍决策。
- **迭代 gate**：主会话每轮亲自跑宪章「机器可判」清单的真实退出码 + 验收报告的未达成计数 + 隔离规模，纪律同 longdev（固定命令、不诊断、产出是布尔值）。
- **授权边界分两层**：宪章可授权（本地 commit / 装依赖 / 起本地服务，问卷勾选）；永不项（push、对外发布、删项目外文件）不进问卷选项，宪章写了也不算。
- **web search 分层**：范围层（要不要做某功能）只在立宪搜一次；迭代中 PO 规划轮对**已选中**的功能做设计层调研（≤3 次搜索/功能，产出 `design/<功能ID>.md`：主流做法、边界情况、明确不学的做法，立项时交给 planner 当调研输入）——搜"这个功能主流怎么做"，不搜"还能做什么"；技术层不搜。调研冒出的范围外想法只进观察区。
- 新增两个 agent：`longdev-product-owner`（继承会话模型，含 WebSearch 做主流形态调研）、`longdev-acceptance`（继承会话模型，先跑后读、拿不准判未达成）。
- longdev 本体这一版零改动——autopilot 只引用不修改，两者同仓同版本发布避免语义错配。

## 0.8.0 — 2026-08-28

**动机**：implementer 自己写、自己跑、自己报「验证通过」，多次出现「假装做好了」。加了独立 reviewer 也没堵住——因为 reviewer 交回来的同样是**叙述**。整条链上每一环都是 LLM 对 LLM 工作的文本声明，没有一个非 LLM 的事实源，所以形不成闭环。这一版把验收的地基从「叙述」换成「机器判定」。

- **主会话第 6 步从「抽查」改成「Gate」**：原来只 `grep` PLAN.md 的阶段状态——那行字是 implementer 自己写的，核对它等于零信息。现在主会话亲自跑：权威验收命令的**真实退出码**、`git diff --stat <SNAP>` 的**真实尺寸**（对 8 文件/400 行的闸，不再只信自报）、测试新增行里的 `skip|xfail|@Ignore|expect(true)` 痕迹、`stages/N.md` 里证据块的存在性。
- **为什么必须是主会话**：subagent 的一切回传都经过它自己转述，**只有主会话自己的工具输出是原始的、用户直接看得见的**。这是整套验收唯一不可伪造的一环，所以明确禁止「派个小 subagent 跑 gate 省 context」。
- **成本实测约 330 token/阶段**（`--stat` 8 文件 ≈ 120，验收命令 `tail -5` ≈ 90，其余 ≈ 120），7 阶段 ≈ 2.3k。而且这不是新开销是**置换**：原来那条 `grep` 状态一样要花 ~100 token，只是换来零信息。
- **新增「Gate 纪律」一节**：会暴增的从来不是字节，是 `exit≠0` 那一刻想去看看为什么的冲动——一次 `cat` 就是 5–10k，角色也从编排者滑成实现者。所以：命令固定不许即兴加；主会话里裸 `git diff` 是禁令；`exit≠0` 不诊断不读文件不改命令重试，直接发回 implementer；不过就走修复轮不重跑变体；全量套件只归 final-reviewer。**gate 的产出是一个布尔值，不是一个任务。**
- **验证段统一成固定证据块**（implementer / reviewer / final-reviewer / stage 模板）：命令原文、真实输出末几行、顶格的 `exit=<码>`，测试框架统计行**原样贴**不许改写成结论。没有 `exit=` 行 = 未验证 = 阻塞。「如实上报」从道德要求降级成**格式要求**。
- **证据块统一要求真实退出码**：使用 `set -o pipefail` 或 PowerShell `$LASTEXITCODE`，保留原始输出和统计行，不能把日志管道的成功码当成被测命令成功。
- **数字交叉核对**：implementer 自报的统计行、reviewer 独立跑出来的统计行、主会话 gate 的退出码，三者要对得上。对不上就是有人在编，当阻塞问题发回。reviewer 的回传摘要因此必须带证据一行，不许写「验证通过」。
- **reviewer 改成「先跑，后读」**：拿到自己的退出码之前不许看 implementer 写的「验证输出」——先看它就被结论锚定了，之后的核对都变成给它找理由。
- **新增阶段起点快照**：派 implementer 前 `git stash create` 打一个游离 commit 对象（不改工作区、不产生 stash 条目），主会话记住这行 SHA 并传给 reviewer。`git diff <SNAP>` 算出来的**恰好是本阶段**，尺寸闸和测试 diff 扫描才有精确口径。
- reviewer 的测试维度加了一条机械扫描（只看 diff 新增行），定位嫌疑处再去读测试代码——不是替代读代码，是让「真去读」有个起点。

**还没做的**（下一版）：完成标准改成「验收命令 → 期望」两栏由 planner 立项时写死；reviewer 加**反证实验**（定点破坏本阶段实现 → 点名的验收测试必须变红，不红说明测试是摆设）；逐阶段 commit。后两项互为前提，一起发。

## 0.7.1 — 2026-08-27
- 把 0.7.0 的降档规则从「主会话是 Fable 时」改成「**继承到比 `opus` 高的模型时**」。今天两者等价（Fable 是唯一在 Opus 之上的档），但按能力档位判定而不是枚举具体型号，以后再出更高档的模型这条规则不用跟着改。

## 0.7.0 — 2026-08-27
- **主会话是 Fable 时，implementer 降一档以 `opus` 派出。** 此前 A.0 建议用 Fable 会话立项、A.6 又明说立项后不用 `/clear`，于是那个 Fable 会话一路带到每一个 implementer——全流程 token 基数最大的角色，跑在最贵的模型上，每个阶段都跑。
- 这不算覆盖用户的会话选择：Fable 会话是**为立项**选的，不是「所有阶段都要 Fable」的声明，是会话自己滑过来的。降档是还原意图。主会话会明说一句，想让某个阶段用 Fable 跑说一声即可。
- planner / decider / final-reviewer **不降**：低频、高判断、基数小。只有 implementer 满足「每阶段都跑 + token 基数最大 + 执行的是已写好的规格」。
- 立下一条规矩：**阶段级 override 只向下，不向上。** 向上靠你在什么会话里跑，向下靠规则——这样永远不会撞上「没有该模型权限」。

## 0.6.2 — 2026-08-27
- **final-reviewer 不再写死 `sonnet`，改为继承主会话。** 按「跑几次 × 判断难度」这个尺度看，它是全流程里定档最不合理的一个：一次任务只跑一次（成本基数小），干的是跨阶段接缝、决策漂移、端到端目标达成这类**综合判断**而不是逐行找 bug，而且它**明确不重跑 `/code-review`**——没有更强的子工具兜底，它就是交付前的最后一道网。
- scout / reviewer 保持 `sonnet`：都是每阶段都跑的高频角色，且 reviewer 把正确性硬活委托给 `/code-review` skill，自己只做清单式一致性核对。
- implementer 保持继承、**不 pin 到任何具体模型**：它是全流程 token 基数最大的角色，执行的又是 planner 已写好的规格（≤8 文件 / ≤400 行，完成标准与风险都已写明），从模型能力提升拿到的边际收益小于开放式角色。要区分强弱应该用阶段级 override，不是全局 pin。

## 0.6.1 — 2026-08-27
- **decider 不再写死模型，改为继承主会话**。0.6.0 写死 `opus` 是设计错误：写死任何具体模型都会让没有该模型权限的人（如 standard team seat 没有 Fable 5）直接用不了，也躲不开额度限制。改成与 planner 同构的「继承主会话 + 提醒一句」。
- 当前会话不是最强模型时，主会话改用「**保守档**」派 decider——prompt 里明确要求把每条升级门槛放宽一档，拿不准就标「需用户」。**降级的是自动化程度，不是正确性**：弱模型下的失败模式变成「多问你几次」，而不是「拍错板」。
- decider 派不出来或回传异常（无权限、撞额度、超时）时**软失败**：退回 v0.5.x 的直接问用户，绝不卡住流程。
- implementer / reviewer 的模型分层不变。

## 0.6.0 — 2026-08-27

**动机**：AFK 时流程阻塞在等人拍板上，人回不来就一直停着。减少「需要惊动人」的次数，比缩短响应延迟更有效。

- 新增 `longdev-decider`（`opus`，独立 context，不参与实现也不给 implementer 背书）。reviewer 揪出的「方案级决策待确认」不再一律阻塞问用户，先由 decider 判类型：
  - **技术判断**（接口形态、数据结构、内部抽象、错误处理位置这类只用「PLAN.md 目标 + 全局决策 + 代码事实」就推得出来的）→ 它代拍，写进 `decisions/agent-<N>-<slug>.md` 备案
  - **意图判断**（要不要覆盖某场景、性能 vs 可维护性）、**后果不可逆**（schema、已发布接口、对外行为）、**依据不足**、**要改 PLAN.md 全局决策** → 一律升级给用户
- **从审批制改成备案制**：代拍的决策落盘 + 汇报时念一行「已代你决定：X，因为 Y」，用户保留否决权但流程不停。三道刹车：未复核代理决策累计 ≥3 停下一次性汇报；动全局决策不代拍；后果不可逆永不代拍（人不在也停）。
- decider 被强制要求写出**指名道姓的依据和回滚方式**，并被明确告知它最可能的失败模式是「把『我不知道』包装成流畅自信的判断」——理由若换到任何别的项目都成立，就必须升级而不是决策。
- implementer 上报「判不准是否不可逆」的分叉时也先过 decider；前置条件不成立、验证反复失败、阶段尺寸超标仍然直接问用户。
- 落盘结构新增 `decisions/`，用户复核后归档到 `decisions/reviewed/`。立项和迁移的 `mkdir` 已同步。

## 0.5.3 — 2026-08-26
- SKILL.md 不再手写版本号：开工报的版本改成运行时读 `.claude-plugin/plugin.json` 的 `version`，plugin.json 成为唯一真源。修之前的漂移：标题写 v0.5.2、正文却让报 `longdev v0.5.0`（0.5.1 / 0.5.2 两次发版都漏改正文那句，实际报出来的是错版本）。
- 发版流程相应简化为只 bump plugin.json 一处。

## 0.5.2 — 2026-08-26

收窄 implementer 的当场打断。原先只要遇到「PLAN.md 和 stages/N.md 都没覆盖的方案级选择」就停下来问用户——口径太宽，一个阶段可能打断好几次，而且**问的时机最差**：那一刻分叉的真实影响还没写出来，用户手上只有一段口头描述。v0.5.1 已经在阶段末给了事后网（reviewer 单列新立决策交用户拍板），事前再拦一道是把同一个人工决策点提前到信息最少的时候。

- **判据改成「我能不能自己挑一个然后有底气往下走」**：能 → 挑、把决策和理由写进 `stages/N.md`、往下做，reviewer 事后揪；不能 → 上报。
- **上报清单里的方案级那条收窄为「返工成本明显超出本阶段」**：不可逆的数据/schema 变更、大范围机械重命名、已对外发布的接口形态。这类挑错了回不来，事后网不适用。判不准是否属于这类时才上报。
- 一般分叉的返工上限被阶段尺寸硬约束封顶（≤8 文件 / ≤400 行），这是敢让它自己定的前提。
- 流程 C.2 措辞同步。

## 0.5.1 — 2026-08-26

补上一个静默的缝：**implementer 自己新立的方案级决策会静默通过审查**。原先 reviewer 维度一只查「实现是否遵守既有决策」和「落盘记录是否属实」——一个 implementer 顺手定下的接口形态或数据结构，只要老实写进 `stages/N.md`，两条检查都过，用户从没被问过。

- **reviewer 维度一新增一条**：逐条审 implementer 新写的决策（含标「阶段 N 提升」的），问它是「从既有决策和代码事实推得的唯一合理选项」还是「一个真分叉」；改变对外接口形态、数据结构、可观察行为、新引入依赖或给后续阶段划死路的，即使记录老实、实现正确也要单列。判不准就列出，不替用户判。
- **报告新增 `## 方案级决策待确认` 一节**，与「阻塞问题」分开：它不是 bug，不该由 implementer 自己改。每条写清定了什么、影响范围、备选、reviewer 的倾向。
- **回传摘要上限 10 → 12 行**，待确认决策每条一行标题，且要写清"它定了什么"——主会话照这行直接问用户。
- **流程 C 新增一步（原 5–9 顺延为 6–10）**：有待确认决策 → 问用户，认可则让 implementer 写实（跨阶段的提升到 PLAN.md 全局决策），否决则按阻塞问题发回修。用户说"你定"当认可，不反复问。
- 硬规矩加一条：新立的方案级决策要过用户，reviewer 只负责揪出来，主会话不代拍。

## 0.5.0 — 2026-08-26

计划文件结构化 + 全角色读写卫生。起因是实测：一个 5 阶段任务里 7 个 implementer 子会话的 context 冲到 200k–600k 字符（60–180k token，最大的已贴近上限会触发压缩），而那份 PLAN.md 长到 65k 字符 / 291 行，其中 ~85% 是阶段局部信息却让每个 implementer 全读。诊断结论是**该拆的是阶段和文档，不是 implementer 的角色**——把实现和测试拆成两个 agent 只会让第二个重读一遍所有文件。

- **一个任务一个目录**：`.claude/longdev/<任务名>/`，含 `PLAN.md`（索引，≤120 行）、`stages/<N>.md`（每阶段入口 + 完成记录，≤80 行）、`notes/<主题>.md`（跨阶段契约类参考物）、`scout/`、`reviews/`。多任务共存不再靠手动改名。
- **PLAN.md 降级为索引**：只放每个阶段都用得上的（目标、基线 commit、阶段表、**全局**决策、**全局**坑、入口级关键文件 ≤15 行、参考物索引、调研指针、验证方式）。阶段局部的决策/坑/文件清单一律留在 `stages/N.md`；只有 implementer 判断跨阶段的才提升到 PLAN.md 并标 `（阶段 N 提升）`。中途冒出的契约类内容进 `notes/`，禁止在 PLAN.md 新开节。
- **各角色只读自己那一份**：implementer N 读 `PLAN.md` + `stages/N.md` + `stages/N-1.md` 的「交接」小节（`sed` 只读该节）+ 入口点名的 notes——从 65k 降到 ~10k；reviewer 读索引 + 本阶段文档 + diff；final-reviewer 读索引 + 全部 stages + 全量 diff（决策漂移检查反而更容易，每阶段决策天然带出处）；主会话只 `grep` 阶段表。
- **阶段尺寸硬约束**：planner 拆解时每阶段 ≤8 个文件 / ≤400 行，超了必须拆成两个。implementer 开工发现超标要先上报，主会话按"拆阶段"处理而不是放行。
- **读写卫生写进全部五个 agent**：>200 行的文件按段读（先 `grep -n` 定位）、禁止 `cat` 整个目录、改文件用 Edit 不用 Write（同一文件 Write 两次以上就是信号）、测试输出走静默 reporter 或 `tail`、**禁止读 `~/.claude/` 下的会话记录/transcript/plans**（实测有 implementer 因入口信息不足去"考古"主会话 transcript，一次吃掉 28k+22k+18k 字符）、同一文件不读第二次。final-reviewer 额外要求按文件读 diff 而不是一把梭。
- **stages 文档两段式**：「入口」由 planner 写、上一阶段 implementer 精修，写完冻结；「完成记录」由本阶段 implementer 追加，含「给下一阶段的交接」——收尾审查靠比对这两段判断阶段是否达成目标。新增 `references/stage-template.md`。
- **旧版计划迁移**（流程 E）：检测到 `.claude/longdev/PLAN.md` 没有配套 `stages/` 目录时，派 planner 走「迁移轮」拆分（只搬运，不重新规划阶段），旧文件留作 `PLAN.legacy.md`。
- 状态判定重写：扫 `.claude/longdev/*/`，多个未完成任务时列出来问用户，不擅自挑。
- reviewer 维度一新增一条：报告「该下沉的内容被塞进 PLAN.md」和文件超行数上限。

## 0.4.1 — 2026-08-26

- SKILL.md 的 description 缩短并前置"做什么"：`/` 命令菜单只显示开头一截，原 150+ 字的描述被截断到看不出用途。触发词保留在句尾。

## 0.4.0 — 2026-08-26

- **立项编排化，不再进 plan mode**：新增 `longdev-planner` agent（继承主会话模型）。主会话派 scout 落文件 → 派 planner 读 scout 报告、按模板写 `PLAN.draft.md`、回传 ≤30 行摘要（阶段清单 / 关键决策 / 待用户拍板 / 风险）→ 主会话把草稿文件展示给用户确认 → 修改意见 `SendMessage` 给同一 planner 迭代 → 确认后 `mv` 转正为 PLAN.md。立项期间主会话只积累摘要，**删除了 v0.3.0 的"立项后建议 /clear"**；全流程唯一需要 `/clear` 的场景只剩 D（主会话亲自执行的阶段）。
- **按客户端选择文件展示方式**：新增 `references/show-file.md`。用 `CLAUDE_CODE_ENTRYPOINT` 判定：CLI 下有 `code`/`cursor` 就 `--reuse-window --goto` 在用户编辑器里打开；裸终端回显绝对路径；非 CLI（桌面 App / 网页）用 `SendUserFile` 推侧栏。立项草稿和收尾 `reviews/final.md` 都走这个机制，主会话自己不读文件。
- **SKILL.md 精简并加版本号**：第一行 `# longdev vX.Y.Z`，主会话开工第一句报版本，用户据此判断是否已更新。删掉解释性的"context 预算"段和历史备注（保留在本文件里），只留操作规则；从 ~140 行压到 ~85 行。
- 状态判定新增"只有 PLAN.draft.md"分支（上次立项没确认完）。

 — 2026-08-26

- `longdev-scout` 模型从 haiku 改为 sonnet（effort medium）。haiku 实测不遵守回传长度约束（40 行上限回了 ~1000 行），调研质量也不稳。

## 0.3.0 — 2026-08-26

- **新增 `longdev-final-reviewer`**：全部阶段完成后做收尾全盘审查。拿 PLAN.md 记录的基线 commit 算整个任务的 diff，查逐阶段 review 结构上抓不到的问题（总目标端到端达成、跨阶段接缝、决策漂移、跨阶段残留），全量跑测试；默认不重跑 `/code-review`（diff ≤800 行或某阶段缺审查记录时才跑 low 档）。SKILL.md C-7/C-8 新增收尾审查与收尾修复轮，最多两轮。
- **测试要求显式化**：立项时阶段完成标准优先写成自动化测试并定下「测试约定」（写进 PLAN.md 验证方式）；implementer 把补单元测试列为交付物，断言行为不断言实现；reviewer 维度一新增测试核对（存在性、锚在完成标准、有无凑绿痕迹）。项目无测试基础设施时"是否引入"是立项决策，不由 implementer 顺手拍板。
- **主会话 context 收缩**（基于实测：一个 5 阶段任务的主会话中，各阶段 implementer/reviewer 回传合计 <20%，其余来自立项调研回传、主会话自己读文件、修复轮复述）：
  - scout / reviewer / final-reviewer 的完整报告一律落文件（`.claude/longdev/scout/*.md`、`reviews/stage-N.md`、`reviews/final.md`），回传只给 ≤15 / ≤10 行摘要；修复轮 `SendMessage` 只传报告路径不粘清单。
  - 立项只派 `longdev-scout`，禁止用 Plan / Explore 等无输出约束的通用 agent 调研；立项完成后建议 `/clear` 再续接（A-7）。
  - 主会话不 `cat` PLAN.md 或任何报告全文，抽查只 `grep` 阶段清单；PLAN.md 内容属实性由 reviewer 核。
- PLAN.md 模板新增「基线 commit」、「测试约定」，决策条目标注来源阶段，「调研结论」只放结论和指向 scout 文件的指针。
- scout / reviewer 的 tools 增加 `Write`。

## 0.2.0 — 2026-08-18

- `longdev-reviewer` 重构为双维度审查：一致性核对（diff vs PLAN.md 阶段目标/决策/入口更新，自己做）+ 代码正确性（调用 `/code-review` skill，medium 档，不带 --fix）。回传的阻塞问题标注来源 `[一致性]` / `[code-review]`。
- reviewer 的 tools 增加 `Skill`；修复轮复查只聚焦上一轮问题，不重跑完整两维度。
- SKILL.md C-3 相应更新，并明确主会话不要替 reviewer 重跑 review。

## 0.1.0 — 2026-08-18

- 首个打包版本。
- 编排模式为默认：主会话只编排，每阶段派干净 context 的 `longdev-implementer` 执行，`longdev-reviewer` 独立审查，全程无需逐阶段 `/clear`。
- 主会话亲自执行降级为例外（阶段标【主会话】时）。
- 状态载体为项目内 `.claude/longdev/PLAN.md`。
