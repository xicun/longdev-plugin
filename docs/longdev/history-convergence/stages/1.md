# 阶段1：历史收敛与统一写入入口

## 入口

**关联**：PLAN.md R01–R08 / V01–V07,V09。
**完成标准**：真实脚本完成候选识别、保守分类/引用闭包、迁移与跟踪后归档、v0.13 后续分叉探测；真实 guard 与角色绝对 TASK_DIR 契约一致；必需证据可携带；独立检查通过后形成本阶段提交。
**前提/基线**：PLAN 已转正；主会话核对基线 `.work/longdev/history-convergence/baselines/` 与父子归属，子起点0c48589、父3b68951均干净。
**范围**：`skills/longdev/scripts/`、两个 SKILL、共享协议/运行时/模板、适用 agents、迁移与安装测试、README/INSTALL/CHANGELOG、双 manifest；父入口仅实际需同步部分。不得改动先前 resume 实现或把其提交推送。
**验证**：PLAN V01–V07,V09；全量命令 `python3 -B -m unittest discover -s tests -v`。各高风险安全分支需行为反例，不能只校验字符串出现。
**参考**：`agents/longdev-implementer.md`、`skills/longdev/references/execution-protocol.md`、现有迁移脚本与 tests。
**风险**：自然语言状态不可靠、未知目录归属、引用传递遗漏、跟踪内容与工作树不一致、归档中断、旧客户端继续写入；按 PLAN 安全回退保留报告。

## 入口变更

- 2026-09-23用户追加迁移--dry-run，新增R08/V09，阶段1同步实现详细只读预演及与实执行的一致性检查。

## 执行与证据

状态：执行中。实现者追加实际改动、决定、精确测试命令及结果，形成可携带 evidence manifest/摘要，原始日志进 .work；只标待检查。
review：`reviews/stage-1.md`；gate：`gates/stage-1.md`。

## 阶段提交与交接

由主会话在 review/gate 后同步记录、核对忽略/跟踪及归属，限定提交；trailer 为 `Longdev-Task: history-convergence`、`Longdev-Stage: 1`。失败不得进入阶段2。
交接实际 commit、v0.14.0 包指纹、CLI/guard 精确入口、有效 V01–V07 与剩余限制，不提前声称已安装。
