# 阶段2：本机升级与发布交付

## 入口

**关联**：PLAN.md R07 / V08；整体核对 R01–R08、V01–V09（含用户追加的迁移 dry-run）。
**完成标准**：本机插件为已验证 v0.14.0 产物，可发现且真实缓存迁移/guard 可运行；子提交已在 Gitee；父仅本轮本地提交，既有 resume 提交未借此发布。
**前提**：阶段1独立 review、主 gate 与本地提交完成；安装前读取 plugin-creator 实际流程。
**范围**：本机原生插件更新、子 Gitee 发布、本任务记录、父本轮关联文件/子模块指针。本阶段不修改已验源码；必要修复返回阶段1受影响检查。
**验证**：PLAN V08；保存原生 CLI 操作退出码与已安装版本/文件哈希、直接缓存 fixture结果、远端准确 SHA；父提交前后核对路径与分支，不运行父 push。
**基线**：主会话记录阶段1 commit、当前插件版本/启用状态及必要备份到 `.work/longdev/history-convergence/baselines/stage-2/`。
**风险**：缓存热加载、安装权限/网络、当前会话技能快照；明确已安装内容与新模型实际遵循是不同证据。远端或安装受阻照实记录，不降低 V08。

## 入口变更

- 无。

## 执行与证据

状态：待执行；记录安装/发布确切命令、结果、指纹、限制。独立 review 为 `reviews/stage-2.md`，主 gate 为 `gates/stage-2.md`。

## 提交与最终交接

review/gate 后先提交本阶段记录，trailer `Longdev-Task: history-convergence`、`Longdev-Stage: 2`；再整体独立审查/最终 gate、收尾记录限定提交与发布，避免把全部阶段记录积压到最后。
最终核对所有 R/V、未知历史内容保留报告、实际提交及同步边界。未经用户接受不标已验收；Windows真机/新模型长期行为缺实测时单列未验证，不冒充测试通过。
