# longdev v0.13.0：阶段提交与持久记录迁移

**目标**：阶段检查通过后自动本地提交；任务记录可随 Git 跨机器续接；新版在当前工作区启动/续接时安全迁移旧记录。
**状态**：执行中
**创建/更新**：2026-09-23
**授权与边界**：用户反馈两项缺陷、讨论自动迁移后明确“那升级吧”，授权升级实现、提交和安装；按项目明确发布约定，子模块独立提交并推送 Gitee，父仓库仅此次 INSTALL/PLAN、bootstrap 恢复路由及对应测试、子模块指针。未授权自动成果验收，不扫描其他工作区，不因迁移启动旧开发任务。
**任务基线**：子仓库 HEAD `bcf726091638becec1ae167ce7d5528e33981357`；主会话确认父/子仓库起点均干净。完整基线：`.work/portable-stage-upgrade/baseline.json`；起始 staged/unstaged/untracked 均空。

## 需求

| R ID | 内容与来源 | 交付物/边界 | V ID | 依赖 | 状态 | 证据 | 剩余 |
|---|---|---|---|---|---|---|---|
| R01 | 用户：阶段不自动 commit 导致积压 | gate 通过、记录同步后本地提交本阶段归属明确的代码与持久记录；保护既有 staged/其他改动；提交失败不得默默推进；push 独立授权 | V01,V05 | 无 | 检查通过 | gates/stage-1.md；fb31edd | 用户验收 |
| R02 | 用户：`.claude/` 被忽略，跨机器不能复用 | 新任务使用 `docs/longdev/`、`docs/autopilot/`；持久计划/状态/决定/审查/gate/交接纳入 Git；备份/原始日志放 `.work/`，持久记录包含可复核摘要 | V02,V04,V05 | 无 | 检查通过 | gates/stage-1.md；fb31edd | 用户验收 |
| R03 | 用户确认启动当前工作区自动迁移 | 技能启动/续接调用实际迁移脚本（位于 `skills/longdev/scripts/`）；仅当前工作区；保留状态、更新引用、冲突不覆盖、可恢复且幂等、检查 Git 忽略；status 只读 | V03,V04 | R02 | 检查通过 | gates/stage-1.md；24/24测试 | 用户验收；Windows真机未验证 |
| R04 | 用户“升级吧” | v0.13.0 版本/变更说明一致；本机原生插件更新并核对已安装内容；按项目约定发布子模块到 Gitee，核对父仓库范围；当前会话旧技能快照限制如实说明 | V04,V06 | R01–R03 | 检查通过 | gates/stage-2.md | 整体检查及记录收尾、用户验收 |

## 验收集合

所有命令在子仓库根执行，使用本机可用 Python 3；测试临时目录固定在项目 `.work/`，不得使用系统 `/tmp`。V 集合为 V01–V06。

| V ID | R ID | 阶段 | 环境 | 方法/确切命令 | 期望结果 | 必需性 |
|---|---|---|---|---|---|---|
| V01 | R01 | 1 | 技能/共享协议/模板 | 逐项审查阶段 gate→同步记录→限定提交→续接顺序；模拟有既有 staged、提交失败、无 Git/无变化的分支 | 不混入他人改动、不伪报提交成功、不绕过 gate；不能提交时明确状态与处理 | 必需 |
| V02 | R02 | 1 | 当前源码与安装薄入口 | `rg -n '\.claude/(longdev|autopilot)|docs/(longdev|autopilot)|baselines|evidence' skills agents scripts README.md INSTALL.md`，逐项分类旧路径是否仅兼容迁移说明 | 新建/续接主入口统一新目录；原始备份日志与持久摘要区分，关键续接信息可随 Git 获取 | 必需 |
| V03 | R03 | 1 | `.work/` 隔离 Git fixture | `python3 -B -m unittest discover -s tests -v`；新增迁移测试覆盖旧记录、内部引用改写与外部引用报告、重复执行、中断恢复、双目录冲突、忽略规则、status 只读、其他工作区不变 | 状态保留、无覆盖丢失、幂等可恢复、忽略可诊断；迁移失败不标成功 | 必需 |
| V04 | R02–R04 | 1 | 安装测试与源码 | 同 V03 全量测试；核对三端 bundle 确实包含迁移脚本，双 manifest 版本一致，启动入口能定位脚本 | v0.13.0 打包完整，既有安装与校验行为无回归 | 必需 |
| V05 | R01,R02 | 1 | 实际父/子 Git 工作区 | `git diff --check`；提交前后 `git status --porcelain=v1 -uall`；提交后 `git show --stat --oneline HEAD`，逐项审查暂存列表与持久记录 | 本阶段经 review/gate 后形成限定范围本地提交，持久记录被跟踪，无无关改动 | 必需 |
| V06 | R04 | 2 | 本机原生插件安装 | 按已读取 plugin-creator 的 CLI cachebuster/reinstall 流程执行，记录实际确切命令；核对安装版本、manifest 与技能/迁移脚本内容指纹及发现入口 | 已安装包为 v0.13.0，内容与当前产物一致；核对 `git ls-remote gitee refs/heads/main`（实际分支以现场为准）能取得发布 commit，父仓库仅限定文件；区分安装验证与尚未发生的新会话真实运行 | 必需 |

## 阶段

| # | 目标 | R / V | 依赖 | 状态 | 入口/记录 | review / gate |
|---|---|---|---|---|---|---|
| 1 | 实现迁移、提交协议与版本更新，独立审查并本地提交 | R01–R04 / V01–V05 | 无 | 检查通过 | stages/1.md | reviews/stage-1.md / gates/stage-1.md |
| 2 | 更新本机插件并核验交付 | R04 / V06 | 阶段 1 检查通过且已提交 | 检查通过 | stages/2.md | reviews/stage-2.md / gates/stage-2.md |

## 全局决策及变更

- 2026-09-23 插入反馈（用户观察，未独立核实原因）：resume中OpenAI session不能共享，DeepSeek与GLM可互用；“内容加密”解释未核实。本次longdev通过项目持久交接恢复，不依赖原生session跨provider兼容；不扩大为resume实现任务。

- 2026-09-23：只改迁移任务树内引用，外部引用命中报告并由当前任务按需处理，避免全仓库业务源/历史文档自动改写；父 bootstrap 路由作为已定位的恢复入口同步更新。

- 2026-09-23：采用两阶段，迁移逻辑与入口/打包测试强耦合，合并到阶段 1；本机安装独立为阶段 2，不为文件数拆开不可独立验证的链条。
- 迁移脚本放 `skills/longdev/scripts/`，现安装 bundle 已包含 skills，避免出现源码可调用而安装后缺脚本。
- 迁移只保持原状态，不将旧“完成”升级成已验收；迁移记录不构成恢复旧任务的授权。
- 依据项目规范发布子模块到 Gitee，主会话核对实际分支与远端可取 commit 后更新父仓库指针。成果交付为待验收；安装/环境阻碍记录具体影响，不降低 V06。

## 当前交接

- 两阶段独立review与主gate通过；V01–V06有有效证据，阶段1代码提交fb31edd已在Gitee main，本机新版安装启用且缓存脚本运行通过。
- 阶段2记录本轮单独提交；下一步整体独立审查、最终记录提交/标签发布及父仓库指针同步。
- 双manifest为0.13.0+codex.20260923092211。父PowerShell回归环境缺口、Windows真机与模型长期任务未验证，见gates/stage-1.md。
- 用户成果验收尚未发生，无已知实现阻塞。
