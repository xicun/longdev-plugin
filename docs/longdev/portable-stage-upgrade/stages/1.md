# 阶段 1：迁移、提交协议与版本更新

## 入口

**关联需求/验收**：PLAN.md 的 R01–R04 / V01–V05。
**完成标准**：新版启动/续接调用实际脚本安全迁移当前工作区；持久任务记录路径统一；阶段检查通过后自动限定提交；v0.13.0 安装包包含脚本；独立 review 与主会话 gate 通过后形成本地提交。
**前置条件**：PLAN 转正；主会话核对父/子 Git 基线和既有改动归属。
**范围与文件**：`skills/longdev/scripts/`、两份 SKILL、references、相关 agents、`scripts/install.py`、测试、README/INSTALL/CHANGELOG、双插件 manifest；按需同步其它引用，不改无关功能。
**验证方法**：PLAN 中 V01–V05；新增必要行为测试；测试临时文件和基线/日志放 `.work/`，持久证据摘要进入任务记录。
**参考/契约**：`skills/longdev/references/execution-protocol.md`；`agents/longdev-implementer.md`；现有 `scripts/install.py` 与 `tests/test_install.py`。
**风险**：新旧目录冲突、忽略规则、迁移中断、相对/绝对路径引用、安装包漏脚本、混入既有暂存文件；全部必须有检查或明确失败处理。
**阶段起点基线**：`.work/portable-stage-upgrade/baseline.json`；HEAD 见 PLAN。

## 入口变更记录

- 2026-09-23：主会话明确“内外引用”边界为迁移树内部自动更新、外部 Markdown/JSON 命中只报告，避免修改业务源/历史资料；不降低保留内容/可核对要求。复杂非 JSON 结构化格式保持原字节，含旧路径时列入 manual_review。

## 执行记录

**实施日期/状态**：2026-09-23 / 待检查。
**实际改动**：新增 `skills/longdev/scripts/migrate_workspace.py` 与 `tests/test_migration.py`；同步两 SKILL、共享协议/运行时/PLAN 与阶段模板、autopilot 迭代模板、planner/implementer/reviewer/final-reviewer 角色、README/INSTALL/CHANGELOG、安装薄入口与打包测试。主会话更新双 manifest 为 `0.13.0+codex.20260923092211`。涉及 R01–R04 的阶段 1 交付范围，不包含本机安装 V06。
**基线与归属**：子仓库任务起点 HEAD `bcf726091638becec1ae167ce7d5528e33981357`，原 staged/unstaged/untracked 均空，原始基线 `.work/portable-stage-upgrade/baseline.json`；当前新增 docs 为本任务规划/审查/验证记录。实施者未提交；主会话 gate 后按归属清单提交。
**局部决定**：迁移脚本仅使用标准库；旧目录保留完整保护副本。原始 baselines 字节不改，日志/媒体及无法 UTF-8 解码或含 NUL 的二进制入 `.work/`。JSON 用标准库解析递归字符串值，Markdown 链接重算相对路径；YAML/TOML 等其它格式不做猜测式改写，含旧路径时报告人工核对。完成标记不依赖本机 `.work/`；后续合法编辑不与旧哈希冲突；不重导入旧目录。
**问题与处理**：独立预审发现命令前缀丢失、JSON 内嵌引号损坏、JSON 相对日志指针悬空；已分别改为路径 token、JSON 标准解析、整值精确路径映射并新增反例测试。增加中断时源文件全集变化检查、Windows drive/reparse 拒绝、无旧记录也检查 docs ignore。修复前验证不支持当前结果，以以下最终自检为准。

## 自检证据

**产物 manifest**：`evidence/implementer-manifest.json`，29 个相关文件 SHA-256，含当前双版本清单；Python 3.11.6 / macOS，cwd 为子仓库根。

| V | 期望与实际 | 结果/剩余 | 原始证据 |
|---|---|---|---|
| V01 / R01 | 静态核对 gate→同步记录→限定 commit→续接；用户禁提交/非 Git/无变化、归属不清/提交失败分支均有明确规则。主会话独立真实 Git fixture 验证 `--only` 只提交 feature+持久PLAN，原 unrelated staged 保留，故意 pre-commit 失败 exit 1 且 HEAD 不变 | 实现者静态通过；真实 fixture 来源为主会话，最终判断待 gate | `.work/portable-stage-upgrade/commit-protocol.json`（主会话） |
| V02 / R02 | 路径扫描逐项分类：新入口/模板为 docs，旧路径仅兼容迁移说明；原始基线/log/图片在 .work，持久摘要/manifest在docs。无旧记录的新项目若 docs 被忽略也退出受阻且无写入 | 自检通过 | `.work/portable-stage-upgrade/implementer-path-audit.log` |
| V03 / R03 | `python3 -B -m unittest discover -s tests -v`：24 tests，20 项迁移行为 + 4 项安装。覆盖只读退出码、内容/状态/命令/JSON/Markdown/Win路径、原始字节、重复/中断/新增源文件/双目录冲突、Git忽略及marker、跨机器无.work、旧源不重导入、symlink/reparse/drive、独占锁、其它工作区不变 | 24/24 自检通过，exit 0 | `.work/portable-stage-upgrade/implementer-tests.log` |
| V04 / R02–R04 | 同全量测试：三客户端 bundle 包含真实迁移脚本并逐字节对照，薄入口为 docs，双manifest版本一致；原安装冲突/升级/移动/校验仍通过 | 自检通过；模型实际新会话启动未在此测试中宣称通过 | 同测试日志及 manifest |
| V05 / R01,R02 | `git diff --check` exit 0；尚未执行实际阶段提交，必须等独立 review 与主会话 gate | 差异自检通过；实际提交/跟踪核验待主会话，不冒称已满足完整V05 | `.work/portable-stage-upgrade/implementer-diff-check.log` |

**限制**：Windows drive/reparse 由跨平台/模拟属性测试覆盖，Windows OS 锁分支尚无真机执行；非 Markdown/JSON 的路径语义、复杂链接及迁移树外引用需按报告核对。迁移不保证历史证据仍有效，原始日志缺失时需针对性复查。脚本不自动 git add/commit；阶段提交由技能执行者在 gate 后实施。
**review / gate**：`reviews/stage-1.md` / `gates/stage-1.md`，主会话核实结论。

## 给下一阶段的交接

- 独立 reviewer 与主会话 gate 已通过，当前等待阶段提交及 V05 跟踪核验，成功后进入阶段 2 安装。版本 `0.13.0+codex.20260923092211`，安装入口 `scripts/install.py`；待主会话补充实际 commit。

## 主会话检查与阶段提交

review/gate 当前证据齐全，阶段检查通过。提交包含本阶段源码与持久记录，原 staged 为空、无无关改动；本记录随阶段commit提交，使用 Longdev-Task: portable-stage-upgrade / Longdev-Stage: 1 定位。实际SHA及提交核验写入下一阶段基线。
